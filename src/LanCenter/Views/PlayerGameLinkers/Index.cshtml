@model IEnumerable<LanCenter.Models.PlayerGameLinker>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>
    
@foreach (var item in Model) {
    <tr>
        <td>
            <a asp-action="Edit" asp-route-id="@item.PlayerGameLinkerID">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.PlayerGameLinkerID">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.PlayerGameLinkerID">Delete</a>
        </td>
    </tr>
}
</table>
